# CI subset of ml/requirements.txt
# Excludes pytorch-tabnet (pulls in torch; tabnet/lstm models are postponed and
# torch-directml is Windows-only). Keep in sync with requirements.txt.
lightgbm==4.5.0
pandas==2.2.3
numpy==1.26.4
scikit-learn==1.5.2
mlflow==2.19.0
snowflake-connector-python[pandas]==3.12.2
pyarrow>=18.0.0
statsmodels>=0.14.4
matplotlib==3.9.2
optuna>=3.6
optuna-integration[mlflow]>=3.6
xgboost>=2.1.0
shap>=0.46.0
